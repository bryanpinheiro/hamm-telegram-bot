API_TOKEN=your_api_token_from_telegram_bot
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your_secure_password
# Public base URL of the metrics subdomain (no trailing slash), used by
# Grafana and Prometheus to build absolute links when served under a sub-path
METRICS_PUBLIC_URL=https://metrics.example.com